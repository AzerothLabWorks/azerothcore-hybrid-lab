diff --git a/src/server/game/Entities/Player/PlayerUpdates.cpp b/src/server/game/Entities/Player/PlayerUpdates.cpp
index 35ac03d79..5175ee97c 100644
--- a/src/server/game/Entities/Player/PlayerUpdates.cpp
+++ b/src/server/game/Entities/Player/PlayerUpdates.cpp
@@ -157,6 +157,11 @@ void Player::Update(uint32 p_time)
 
     m_achievementMgr->Update(p_time);
 
+    // Hybrid QA: recover from stale casting state that can otherwise suppress
+    // melee auto attacks after instant/channel-heavy hybrid combat.
+    if (HasUnitState(UNIT_STATE_CASTING) && !IsNonMeleeSpellCast(false, false, true))
+        ClearUnitState(UNIT_STATE_CASTING);
+
     if (HasUnitState(UNIT_STATE_MELEE_ATTACKING) && !HasUnitState(UNIT_STATE_CASTING) && !HasUnitState(UNIT_STATE_CHARGING))
     {
         if (Unit* victim = GetVictim())
diff --git a/src/server/game/Handlers/CombatHandler.cpp b/src/server/game/Handlers/CombatHandler.cpp
index 7f945eb17..b3226b9a0 100644
--- a/src/server/game/Handlers/CombatHandler.cpp
+++ b/src/server/game/Handlers/CombatHandler.cpp
@@ -62,6 +62,11 @@ void WorldSession::HandleAttackSwingOpcode(WorldPacket& recvData)
         }
     }
 
+    // Hybrid QA: manual attack should recover if a previous spell left the
+    // player in casting state without an active non-melee spell.
+    if (_player->HasUnitState(UNIT_STATE_CASTING) && !_player->IsNonMeleeSpellCast(false, false, true))
+        _player->ClearUnitState(UNIT_STATE_CASTING);
+
     _player->Attack(pEnemy, true);
 }
 
diff --git a/src/server/game/Spells/Spell.cpp b/src/server/game/Spells/Spell.cpp
index 68f028a95..99d1edbd4 100644
--- a/src/server/game/Spells/Spell.cpp
+++ b/src/server/game/Spells/Spell.cpp
@@ -2917,8 +2917,17 @@ void Spell::DoAllEffectOnTarget(TargetInfo* target)
         if (missInfo != SPELL_MISS_EVADE && !m_caster->IsFriendlyTo(effectUnit) && (!m_spellInfo->IsPositive() || m_spellInfo->HasEffect(SPELL_EFFECT_DISPEL)))
         {
             if (!m_triggeredByAuraSpell.spellInfo || m_damage || (!(m_triggeredByAuraSpell.spellInfo->Effects[m_triggeredByAuraSpell.effectIndex].TriggerSpell == m_spellInfo->Id) && !(m_triggeredByAuraSpell.spellInfo->IsAuraEffectEqual(m_spellInfo))))
+            {
                 m_caster->AtTargetAttacked(effectUnit, !(m_spellInfo->HasAttribute(SPELL_ATTR1_NO_THREAT) || m_spellInfo->HasAttribute(SPELL_ATTR3_SUPPRESS_TARGET_PROCS)));
 
+                // Hybrid QA: melee-class player spells should keep white swings active even if
+                // the client drops or misses its /startattack swing packet.
+                if (caster->IsPlayer() &&
+                    m_spellInfo->DmgClass == SPELL_DAMAGE_CLASS_MELEE &&
+                    !m_spellInfo->HasAttribute(SPELL_ATTR0_CANCELS_AUTO_ATTACK_COMBAT))
+                    caster->Attack(effectUnit, true);
+            }
+
             // Spells that don't initiate combat shouldn't affect stand state (e.g. Premeditation, Storm Giant/Felreaver Passive)
             if (m_spellInfo->HasInitialAggro() && !effectUnit->IsStandState())
                 effectUnit->SetStandState(UNIT_STAND_STATE_STAND);
